import React from 'react';
import classname from 'classnames';
import style from './Style.module.scss';

const SetupButton = ({ children }: { children: React.ReactNode }) => {
  return <div></div>;
};

export default SetupButton;
