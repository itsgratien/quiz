mutation AddMoreAttendant($args: [AddAttendantArgs!]!, $testId: String!) {
  addMoreAttendant(args: $args, testId: $testId) {
    data
    message
    error
    items {
      _id
      names
      email
      phoneNumber
      createdAt
      updatedAt
    }
  }
}
