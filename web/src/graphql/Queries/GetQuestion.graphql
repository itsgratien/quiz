query GetQuestion($id: String!) {
  getQuestion(id: $id) {
    data {
      _id
      title
      type
      createdAt
      updatedAt
      slug
      status
      answers
      choices
      description
    }
    totalDocs
    totalPages
    error
    message
  }
}
