query GetUser {
  getUser {
    errors {
      ...errors
    }
    error
    data {
      ...user
    }
  }
}

fragment errors on ErrorsT {
  field
  message
}

fragment user on User {
  _id
  username
  names
  email
}
